import planDocumentSchema from "./schemas/plan-document.schema.json" with { type: "json" };

/** Draft 2020-12 schema for persisted PlanDocument v2 packages. */
export const PLAN_DOCUMENT_V2_SCHEMA = planDocumentSchema;
