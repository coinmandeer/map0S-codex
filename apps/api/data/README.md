# Czech admin regions (simplified)

`cz-regions.geojson` is a bbox-simplified FeatureCollection of 14 kraje + 76 okresy (plus Praha).
Canonical source used by the API is `apps/api/src/data/czRegions.ts`.
